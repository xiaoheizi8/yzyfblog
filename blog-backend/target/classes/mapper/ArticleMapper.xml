<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.ArticleMapper">
    <select id="searchByKeyword" resultType="com.blog.entity.Article">
        SELECT * FROM blog_article
        WHERE deleted = 0 AND status = 1
        <if test="keyword != null and keyword != ''">
            AND (MATCH(title, content) AGAINST(#{keyword} IN NATURAL LANGUAGE MODE)
            OR title LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY publish_time DESC
    </select>
    <select id="selectRecommend" resultType="com.blog.entity.Article">
        SELECT * FROM blog_article
        WHERE deleted = 0 AND status = 1
        <if test="excludeId != null">
            AND id != #{excludeId}
        </if>
        ORDER BY view_count DESC, like_count DESC
        LIMIT #{limit}
    </select>
</mapper>
