<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.CommentMapper">
    <select id="selectTreeByArticleId" resultType="com.blog.model.entity.Comment">
        SELECT * FROM blog_comment
        WHERE article_id = #{articleId} AND deleted = 0 AND status = 1
        ORDER BY parent_id ASC, create_time ASC
    </select>
</mapper>
